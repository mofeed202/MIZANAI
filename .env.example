# Public client configuration only
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Secrets for server/Edge Functions belong in Supabase/provider secrets and must never be exposed to the browser.
